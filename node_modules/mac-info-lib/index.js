const os = require('os');
class MacInfo {
    totlaMacMem() {
        console.log('availble mem',os.totalmem());
    }

    freeMacMem() {
        console.log('free mem',os.freemem());
    }

    macType() {
        console.log('mac type',os.type());
    }
}
module.exports = MacInfo;